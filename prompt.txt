Read renderer/apps/emergence.js generate(). The diagnostic confirmed: accumulating raw avatar bitmaps always yields the average Normie face. Replace WHAT is accumulated. Keep the stochastic sampling, distinctiveness, threshold, cleanup, AbortController, progressive paint, and 1-bit render.

POOL FIX:
- Paginate /agents/list to build a pool of up to 500 agents (limit=100 across pages/cursors), cache as 'agents_pool'. Draw 80 via the existing weighted sampler.

ACCUMULATION FIX — stop stacking faces, deposit per-agent on-chain glyphs:
For each drawn agent, derive a deterministic glyph from its on-chain data, NOT its avatar bitmap:
- seed = hashStr(tokenId + '-' + sessionSalt)
- prng = createPRNG(seed)
- center cell: cx,cy derived from (tokenId hash) mod 40 — each agent occupies a distinct region
- glyph size/density driven by traits: radius = clamp(2 + level/3, 2, 6); intensity = weight (prestige+burns based)
- shape style chosen by agent type: human=filled disc, cat=ring, alien=scatter cluster, agent=cross/plus
- stamp the glyph into accGrid at (cx,cy) with intensity, using prng for organic edge variation
This way each agent contributes a distinct mark at a distinct location; 80 different agents = 80 different marks = a unique composition every generation.

After accumulation: keep freq-based distinctiveness dampen/boost, normalize, threshold (~30-40% fill), one majority-filter cleanup pass, render.

Emit emergence:complete with agentCount. Output a diff and tell me the threshold value you used.