Let's streamline the Mainframe (Control Panel) by removing redundant tabs and unnecessary system information, keeping it ultra-minimalist.

In 'renderer/apps/controlpanel.js', please make the following surgical removals:

1. REMOVE THE 'NETWORK' TAB COMPLETELY:
   - In the HTML template (the `body.innerHTML` block), delete the button: `<button class="window-tab-btn" data-panel="cp-network">[ NETWORK ]</button>`.
   - In the HTML template, delete the entire `<div class="tab-content-panel" id="cp-network"> ... </div>` block.
   - Delete the click event listener associated with `#cp-offline-box` (the offline mode toggle) further down in the file.
   - Delete the async function that pings the API (`const pingEl = body.querySelector('#cp-ping-val'); ...`).

2. CLEAN UP 'SYSTEM INFO' DATA:
   - In the `renderSysInfo` function, remove the redundant lines from the returned array so it ONLY displays the following core technical data:
     OS, VERSION, CPU, GPU, RAM, and RES.
   - Specifically, delete these lines from the `renderSysInfo` array:
     `>> ${pad('UPTIME', 9)}: ${fmtUptime()}`
     `>> ${pad('NETWORK', 9)}: ${network}`
     `>> ${pad('WALLET', 9)}: ${walletShort}`
     `>> ${pad('NORMIES', 9)}: ${window.allNormieIds?.length ?? '?'}`
     `>> ${pad('PRESTIGE', 9)}: ${window.walletPrestige ?? '?'}`
   - You can also safely delete the `fmtUptime` and `walletShort` helper variables defined just above `renderSysInfo`, as they are no longer needed.

Apply these changes to make the Mainframe lean and strictly focused on hardware/AI configuration.