EMERGENCE now varies correctly (good). But output is too sparse — only ~8 glyphs render instead of 80, reading as scattered icons. Fix density + connect into a constellation. Read renderer/apps/emergence.js generate().

1. DRAW ALL AGENTS: confirm every one of the ~80 drawn agents stamps a glyph, not just type representatives. Log the actual placed-glyph count. If the distinctiveness threshold is killing most marks, lower it so ~50-65 glyphs survive (aim for a populated but not saturated field).

2. CONSTELLATION MESH (the key visual): after placing all glyph centers, store them in an array. Then draw faint connection lines between centers that are within 12 cells of each other, reusing the proximity-line approach from hive.js NETWORK tab:
   - ctx.lineWidth = 1
   - for each pair, if distance < 12 cells: ctx.beginPath(); opacity = 0.15 * (1 - dist/12); strokeStyle rgba(72,73,75,opacity); moveTo/lineTo between the two centers; stroke()
   - ctx.beginPath() per line (avoid path leak)
   This turns scattered glyphs into one connected web — a unique constellation per generation.

3. Keep glyphs small (radius 2-3) so individual marks stay crisp and the lines read clearly.

Keep stochastic sampling, decorrelated cx/cy, distinctiveness, AbortController, progressive paint, 1-bit #48494b/#e3e5e4. Output a diff and the placed-glyph count.