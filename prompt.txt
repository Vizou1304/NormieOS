REMOVE 'GUARD' FROM THE START MENU:
   - Locate the Start Menu app grid generation logic or the native apps registration array (likely in 'renderer/index.html' or your main OS script).
   - Find the configuration, array object, or hardcoded HTML block for the 'GUARD' app (its key is likely 'normieguard' or 'guard').
   - Completely remove it so the shield icon and the "GUARD" label no longer appear in the Start Menu grid.