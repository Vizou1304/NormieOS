Fix the data mapping bugs in renderer/services/MarketPulseService.js and renderer/apps/market.js. The UI looks perfect, but several stats are missing or incorrect:

1. Fix LISTED %: OpenSea v2 stats JSON doesn't have a flat '.listed' key. Calculate it dynamically using:
   const listedPercent = stats.total_supply ? ((stats.num_listed || stats.listings_count || 0) / stats.total_supply * 100).toFixed(1) + '%' : '6.5%';
   Ensure this value maps correctly to the UI cell.

2. Fix SACRIFICES TODAY: The timestamp logic is failing. Ensure you are handling block timestamps correctly just like in briefing.js. If the API timestamp is in seconds, multiply by 1000 before comparing it to the 24-hour cutoff (Date.now() - 24 * 3600000).

3. Fix BEST SALE Fallback: If OpenSea events fail to return the single highest daily sale, parse the recent sales from the stats object or use your local execution state cache as a safe baseline instead of leaving it as '--'.

Do NOT touch any CSS, layout structural properties, or the window header buttons. Only fix the data injection logic.